<span
    class=" {{ html_classes(
        'w-full',
        'flex items-center justify-center',
        'bg-white',
        {
            'bg-yellow-300': cell.treasure,
            'animate-pulse': cell.has_player
        }
    ) }}"
>
    C
</span>