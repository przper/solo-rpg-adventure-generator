<div class="relative">
    <span
        class=" {{ html_classes(
            'w-full',
            'flex items-center justify-center',
            'bg-white',
            'scale-125',
            {
                'bg-yellow-300 border border-yellow-300': cell.treasure,
                'bg-red-500': cell.enemies,
                'animate-pulse': cell.hasPlayer
            }
        ) }}"
    >
        R
    </span>
</div>
