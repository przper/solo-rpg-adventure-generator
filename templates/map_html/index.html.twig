<div class="mb-4 p-4 rounded-xl shadow-sm bg-white">
    <div class="p-6 text-xs bg-black">
        {% for column in cells %}
            <div class="flex items-center justify-center">
                {% for cell in column %}
                    <div class="w-6 h-6 flex-none">
                        {{ include(cell.template) }}
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
    </div>
</div>
